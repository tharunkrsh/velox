services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./.cache:/app/.cache
    environment:
      - PYTHONUNBUFFERED=1

  dashboard:
    build:
      context: ./dashboard
      dockerfile: Dockerfile
    ports:
      - "5173:5173"
    depends_on:
      - api